{% extends 'base.html.twig' %}

{% block title %}SAV{% endblock %}

{% block body %}
    <h1>SAV</h1>
    <p>Pour nous faire parvenir une demande de sav veuillez remplir le formulaire ci-dessous</p>

    {{ form_start(form) }}

        <div class=" text-primary mt-3 fs-4">
            {{ form_row(form.NumeroDeCommande, {"label": "N° commande"}) }}
        </div>

        <div class="fs-4 text-primary mt-3">
            {{ form_row(form.email) }}
        </div>

        <div class="text-danger mt-3">
            {{ form_row(form.motif) }}
        </div>

        <div class="text-warning mt-3">
            {{ form_row(form.message) }}
        </div>

        <div class="mt-3">
            {{ form_row(form.envoyer, {"attr": {'class':'formSav'}}) }}
        </div>

    {{ form_end(form, {'render_rest': true}) }}

{% endblock %} 
