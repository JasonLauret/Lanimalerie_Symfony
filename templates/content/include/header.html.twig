<header>
    <div class="container-fluid pb-3 pt-3">
        <div class="row">
            <!--___________réseaux sociaux___________-->
            <div class="col-md-4 d-none d-md-block">
                <a href="https://www.facebook.com"><img class="facebook" src="{{ asset('img/logo/logo_facebook.png') }}" alt="logo facebook"></a>
                <a href="https://www.instagram.com"><img class="instagram" src="{{ asset('img/logo/logo_instagram.jpg') }}" alt="logo instagram"></a>
                <a href="https://www.twitter.com"><img class="twitter" src="{{ asset('img/logo/logo_twitter.png') }}" alt="logo twitter"></a>
            </div>
            
            <!--______Mon compte, mon panier, gestion______-->
            {% if is_granted('ROLE_ADMIN') %}
            <div class="col-12 col-md-8">
                <div class="d-flex flex-column align-items-center align-items-md-end">
                    <div>
                        <p class="m-0 pb-2"><span class="fw-bold">Bonjour {{ app.user.firstName }}</span>. Vous ètes connecter en tant qu'Admin.</p>
                    </div>
                    <div class="d-flex">
                        <div class="right-menu">
                            <button class="menu-button fs-5 ms-2 m-0"><i class="pe-2 bi bi-gear"></i>Gestion</button>
                            <div class="dropdown-menu">
                                <a class="" href="{{ path('user') }}">Utilisateurs</a>
                                <a class="" href="{{ path('brand_index') }}">Marques</a>
                                <a class="" href="{{ path('all_productAdmin') }}">Produits</a>
                                <a class="" href="{{ path('all_category') }}">Catégories</a>
                                <a class="" href="">Sous-catégorie</a>
                            </div>
                        </div>
                        <div class="d-flex">
                            <div>
                                <a href="{{ path('cart') }}"><button class="menu fs-5 ms-2 m-0"><i class="pe-2 bi bi-basket2"></i>Panier</button></a>
                            </div>
                            <div class="right-menu">
                                <button class="menu-button fs-5 ms-2 m-0"><i class="pe-2 bi bi-gear"></i>Mon compte</button>
                                <div class="dropdown-menu">
                                    <a class="" href="{{ path('app_setting') }}"><i class="bi bi-gear me-1"></i>Paramètre</a>
                                    <a class="" href="{{ path('app_logout') }}"><i class="bi bi-door-open me-1"></i>Deconnecter</a>
                                </div>
                            </div> 
                        </div>
                    </div>
                </div>
            </div>
            {% elseif app.user %}
            <div class="col-12 col-md-8 d-flex justify-content-between justify-content-md-end">
                <div class="d-none d-md-block">
                    <p class="m-0 mt-2"><span class="fw-bold">Bonjour {{ app.user.firstName }}</span></p>
                </div>
                <div class="d-flex">
                    <div>
                        <a href="{{ path('cart') }}"><button class="menu fs-5 ms-3 m-0"><i class="pe-2 bi bi-basket2"></i>Panier</button></a>
                    </div>
                    <div class="right-menu">
                        <button class="menu-button fs-5 ms-2 m-0"><i class="pe-2 bi bi-gear"></i>Mon compte</button>
                        <div class="dropdown-menu">
                            <a href="{{ path('app_setting') }}"><i class="bi bi-gear me-1"></i>Paramètre</a>
                            <a href="{{ path('app_logout') }}"><i class="bi bi-door-open me-1"></i>Deconnecter</a>
                        </div>
                    </div> 
                </div>
            </div>
            {% else %}
            <div class="col-12 col-md-8 d-flex justify-content-between justify-content-md-end">
                <a {% if app.user %}href=""{% else %}href="{{ path('cart') }}"{% endif %}>
                    <button class="menu fs-5 ms-md-3 m-0"><i class="pe-2 bi bi-basket2"></i>Panier</button>
                </a>
                <a href="{{ path('app_login') }}"><button class="menu fs-5 m-0 ms-2"><i class="pe-2 bi bi-person-fill"></i>Me connecter</button></a>
                
            </div>
            {% endif %}
        </div>
    </div>

    <!--_____________Logo________________-->
    <div class="mt-3 mb-5 d-flex justify-content-center">
        <a href="{{ path('home') }}"><img class="img_logo" src="{{ asset('img/logo/logo-chatChien_3.jpg') }}"></a>
    </div>
</header>