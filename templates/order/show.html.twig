{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}

    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ order.user.lastName }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ order.user.firstName }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ order.date ? order.date|date('Y-m-d') : '' }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('order_index') }}">back to list</a>

    <a href="{{ path('order_edit', {'id': order.id}) }}">edit</a>

    {{ include('order/_delete_form.html.twig') }} #}


    <section class="container mt-3">
    <h1 class="mb-2">Détail de la marque</h1>
    <table class="table">
        <tbody class="structure">
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ order.user.lastName }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ order.user.firstName }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ order.date ? order.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content-center mb-4 border-none">
        <a class="lien" href="{{ path('order_index') }}">Revenir à la liste</a>
        <a class="lien mx-2" href="{{ path('order_edit', {'id': order.id}) }}">Modifier</a>
        {{ include('order/_delete_form.html.twig') }}
    </div>
</section>
{% endblock %}
