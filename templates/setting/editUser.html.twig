{# {% extends 'base.html.twig' %}

{% block title %}Modifier mes informations{% endblock %}

{% block body %}

<section class="">
    <div class="container py-5">
        <h1 class="">Modifier mes informations</h1>
        {{ form_start(form) }}

            <div class="row">
                <div class="col-12 col-md-6">
                    <div class="pb-3">
                        {{ form_row(form.lastName, {"label": "Nom", "attr": {'class':'form-control', 'placeholder':'Dupond'} }) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.firstName, {"label": "Prénom", "attr": {'class':'form-control', 'placeholder':'Aurélie'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.email, {"label": "Email", "attr": {'class':'form-control', 'placeholder':'exemple@exemple.com'}}) }}
                    </div>
                    <div class="p-2 border border-muted rounded">
                        {{ form_row(form.civility, {"label": "Civilité", "attr": {'class':''}}) }}
                    </div>
                    <div class="pb-3 mt-3 mt-md-4">
                        {{ form_row(form.birthDate, {"label": "Date de naissance", "attr": {'class':'motif'}}) }}
                    </div>
                </div>

                <div class="col-12 col-md-6">
                    <div class="pb-3">
                        {{ form_row(form.country, {"label": "Pays", "attr": {'class':'form-control'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.city, {"label": "Ville", "attr": {'class':'form-control'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.postalCode, {"label": "Code postale", "attr": {'class':'form-control'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.address, {"label": "Adresse 1", "attr": {'class':'form-control'}}) }}
                    </div>
                    
                </div>
            </div>

            <div class="mt-3 text-center">
                <a class="lien" href="{{ path('setting') }}">Retour</a>
                <button type="submit" class="lien_2">Modifier</button>
            </div>

        {{ form_end(form, {'render_rest': false}) }}
    </div>
</section>

{% endblock %}

 #}

 {% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}

<section class="">
    <div class="container py-5">
        <h1 class="">Modifier un utilisateur</h1>
        {{ form_start(form) }}

            <div class="row">
                <div class="col-12 col-md-6">
                    <div class="pb-3">
                        {{ form_row(form.lastName, {"label": "Nom", "attr": {'class':'form-control', 'placeholder':'Dupond'} }) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.firstName, {"label": "Prénom", "attr": {'class':'form-control', 'placeholder':'Aurélie'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.email, {"label": "Email", "attr": {'class':'form-control', 'placeholder':'exemple@exemple.com'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.civility, {"label": "Civilité", "attr": {'class':''}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.birthDate, {"label": "Date de naissance", "attr": {'class':'motif'}}) }}
                    </div>
                </div>

                <div class="col-12 col-md-6">
                    <div class="pb-3">
                        {{ form_row(form.country, {"label": "Pays", "attr": {'class':'form-control'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.city, {"label": "Ville", "attr": {'class':'form-control'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.postalCode, {"label": "Code postale", "attr": {'class':'form-control'}}) }}
                    </div>
                    <div class="pb-3">
                        {{ form_row(form.address, {"label": "Adresse", "attr": {'class':'form-control'}}) }}
                    </div>
                </div>
            </div>

            <div class="mt-3 text-center">
                <a class="lien" href="{{ path('setting') }}">Revenir à la liste</a>
                <div class="pb-3">
                    {{ form_row(form.valider, {"label": "Valider"}) }}
                </div>
            </div>

        {{ form_end(form, {'render_rest': false}) }}
    </div>
</section>

{% endblock %}
