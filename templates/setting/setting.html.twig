{% extends 'base.html.twig' %}
{% block body %}

<section class="container my-5">
    <div class="row">
        <div class=" col-lg-3">
        
        </div>
        <p class="h2 col-12 text-start col-md-6 pb-3">Mes informations</p>
    </div>
    
    <div class="row">
        <div class="col-12 text-center col-lg-3 text-lg-start flex-lg-column pt-3 border rounded">
            <p><a class="lien" href="{{ path('edit_user', {'id': app.user.id}) }}">Modifier mes informations</a></p>
            <p><a class="lien" href="{{ path('adress_index', {'id': app.user.id}) }}">Mes adresses</a></p>
            <p><a class="lien" href="{{ path('all_user_order', {'id': app.user.id}) }}">Voir mes commandes</a></p>
            <p><a class="lien" href="{{ path('edit_password', {'id': app.user.id}) }}">Changer mon mot de passe</a></p>
        </div>
        <table class="border rounded shadow ms-1 pt-4 col-12 col-lg-6">
            <tbody class="fs-5">
                <tr class="bg-light">
                    <th>Genre:</th>
                    <td>{{ app.user.civility }}</td>
                </tr>
                <tr>
                    <th>Prénom:</th>
                    <td>{{ app.user.firstName }}</td>
                </tr>
                <tr class="bg-light">
                    <th>Nom:</th>
                    <td>{{ app.user.lastName }}</td>
                </tr>
                <tr>
                    <th>Email:</th>
                    <td>{{ app.user.email }}</td>
                </tr>
                <tr class="bg-light">
                    <th>Date de naissance:</th>
                    <td>{{ app.user.birthDate ? app.user.birthDate|date('d/m/Y') : '' }}</td>
                </tr>
            </tbody>
        </table>
    </div>
</section>

<!--________Back to top_______-->

    <div id="scrollUp">
        <a href="#top"><i class="bi bi-arrow-up-short"></i>Haut de page<i class="bi bi-arrow-up-short"></i></a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
{% endblock %}