{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}
    <h1>Brand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
            {% if brand.logo %}
            <tr>
                <th>Logo</th>
                <td><img src="{{ asset('upload/' ~ brand.logo) }}" alt="{{ brand.name }}"></td>
            </tr>
            {% endif %}
        </tbody>
    </table>

    <a href="{{ path('brand_index') }}">Revenir à la liste</a>

    <a href="{{ path('brand_edit', {'id': brand.id}) }}">Modifier</a>

    {{ include('brand/_delete_form.html.twig') }}
{% endblock %}
