<form method="post" action="{{ path('adress_delete', {'id': adress.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adress.id) }}">
    <button class="btnAddress2 pt-0">Oui, supprimer</button>
</form>
